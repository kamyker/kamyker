# Agent notes

## Show the site locally

To preview the website, just run `run.bat`.

That starts Vite and opens the browser. Do not invent a custom `npm`/`npx`/`node` launch, background process, or PATH workaround. `run.bat` is the way.
